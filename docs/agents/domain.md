# Domain docs

This is a single-context repository.

## Before exploring

- Read root `CONTEXT.md` when it exists.
- Read relevant decisions under `docs/adr/` when they exist.
- If either location is absent, proceed without requiring it.

Use vocabulary defined by `CONTEXT.md`. Surface any conflict with an existing
ADR instead of silently overriding the decision.
